# agents/ — Autonomous AI Processes [NOVEL]

> **No Linux equivalent** — this is a novel extension.
>
> Autonomous agent runtime that executes isolated kernel instances,
> optimizing for human coherence (H-score) rather than CPU utilization.

## Contents

| File | Purpose |
|---|---|
| `q-agent.ts` | Agent lifecycle management, mesh orchestration |
| `q-agent-projection.ts` | Agent → holographic surface projection frames |
| `procedural-memory.ts` | Habit formation and reward-based learning |
| `mirror-protocol.ts` | Inter-agent coherence bonding and empathy |

## Key Concepts

- **H-score inversion** — agents optimize for *human* coherence, not self-confidence
- **Session chains** — hash-linked sequence of agent state snapshots
- **Procedural memory** — repeated successful patterns crystallize into habits
- **Mirror bonds** — agents that predict each other's H-score form empathy links
